// key is what we use to listen for events
// value is an arbitrary (unique) string used by the browser

export default {
  window: {
    scroll: 'window.scroll',
    navigation: 'window.navigation'
  }
}